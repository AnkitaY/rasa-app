# S02 Sprint Plan
# TOKEN BUDGET: 600 | Read by: lead-engineer, feature-execution-skill
# PM review: docs/superpowers/specs/2026-05-13-s02-ux-pm-review.md
# UX specs: docs/ux/ — each FEAT references its spec file(s)

Sprint: S02 | Goal: Breakfast/Lunch/Dinner meal types, day-first planner, today-focused home card, bank-first generation

---

## Dependency order

```
FEAT-S02-000 (schema) → FEAT-S02-001, 002, 004
FEAT-S02-003 (streaming + loading screen) — independent
FEAT-S02-005 (onboarding Q1) — independent
FEAT-S02-006 (recipe prep section) — independent
FEAT-S02-007 (recipe bank import) — independent
FEAT-S02-008 (bank-first generation) — after 000 and 007
BUG-013 — fix alongside features
```

---

## FEAT-S02-000 — Schema migration (prerequisite)

**Blocks:** FEAT-S02-001, FEAT-S02-002, FEAT-S02-004

- [ ] [DB] Supabase migration: rename `meals.meal_type` enum values `'brunch'→'breakfast'`, add `'lunch'`; rename `user_preferences.brunch_days→breakfast_days`, add `lunch_days` column (integer, default 0); migrate all existing `meal_type='brunch'` rows to `'breakfast'`. Follow supabase-patterns skill. Read all existing migrations in `supabase/migrations/` before writing.
- [ ] [API] Search codebase for hardcoded `'brunch'` strings in route handlers, AI prompts, and type definitions. Replace with `'breakfast'`. Run `npm run build` — must pass clean.

---

## FEAT-S02-001 — Day Selection Widget

**Spec:** `docs/ux/day-selection-widget.md`
**Depends on:** FEAT-S02-000

- [ ] [UI] Build `DaySelectionWidget` component: 8-column grid (52px day label col + equal-flex meal type columns); row height 44px; column headers (Bfast / Lunch / Dinner); toggle cell — ON: 24×24px filled square, p1-terra bg, white checkmark; OFF: p1-border border, p1-cream bg; full 44×44pt tap target per cell; all toggles ON by default on first render; pre-fill from prior week's selection if available.
- [ ] [UI] Integrate into planning form: widget appears below meal-type chips, replaces existing days stepper; widget hidden when 0 chips selected; chip added → new column appears (all ON); chip removed → column disappears; live summary row below grid (e.g. "5 Breakfast days · 3 Dinner days"); inline validation note if all toggles OFF for a chip-selected meal type; "Generate my plan" button disabled while validation error exists.

---

## FEAT-S02-002 — Planner day-first layout + visual refresh + empty slot

**Specs:** `docs/ux/planner-day-layout.md`, `docs/ux/planner-visual-refresh.md`
**Depends on:** FEAT-S02-000
**Note:** Replaces the current meal-type-grouped planner sections (BRUNCH THIS WEEK / DINNERS THIS WEEK). The CTA layout in planner-day-layout.md is overridden by the PM review — use the interaction table in `docs/superpowers/specs/2026-05-13-s02-ux-pm-review.md` (UX-022 section) instead of the spec's 5-CTA layout.

- [ ] [UI] Restructure planner page: day-first sections Monday–Sunday; p1-terra full-bleed day header (text-base, font-bold, white); secondary text right-aligned on header ("All done ✓" or "N of M cooked"); gap-3 between cards within a day; mb-4 between day sections; p1-cream page background.
- [ ] [UI] Meal card redesign: entire card tappable → navigates to `/recipes/[recipe_id]`; ✕ icon top-right (26px circle, p1-cream bg, p1-brown icon) → Skip (soft-delete slot, slot becomes empty); ⇄ icon button (38×38px, p1-border, rounded-lg) → opens swap sheet; checkbox (28×28px, rounded-md, p1-border) + "Cooked" label below → Mark as Cooked; "Start Cooking" primary button (p1-terra, flex-1, h-38px); action buttons use `stopPropagation` to prevent card-tap navigation; Playfair Display recipe name (text-xl, font-bold, p1-dark); subtitle text (text-sm, p1-brown); time metadata top-right of meta row (text-xs, p1-brown); meal type chip top-left (p1-surface bg, p1-brown text, text-xs, pill).
- [ ] [UI] Cooked state card: p1-forest-lt (#E2EDE6) background; recipe name Playfair Display text-base font-semibold p1-forest; "✓ Cooked" badge (p1-forest, text-xs); all action buttons hidden; card stays full height.
- [ ] [UI] Rest day row: "Rest day" text (p1-brown, text-sm, px-4 py-3), no card surface, no border.
- [ ] [UI] Empty slot card: 1.5px dashed p1-border border; p1-surface background; rounded-xl; meal type chip top-left; "+ Add a meal" centered (text-sm, p1-brown); entire card tappable → triggers in-place generation.
- [ ] [API/DB] `/api/meals/generate-single` endpoint: POST with `{ week_plan_id, day, meal_type }`; runs AI generation for one slot using bank-first logic; returns the saved meal row. Create if not exists.
- [ ] [UI] In-place generation loading state: dashed card → solid border + "Finding something for [Day] [Meal]..." + two animate-pulse skeleton bars; rest of planner stays interactive; on success replace with full meal card (no scroll jump); on error revert to empty slot with "Couldn't find something — try again?" and "Try again" text link (p1-terra).

---

## FEAT-S02-003 — Generation loading screen + streaming

**Spec:** `docs/ux/generation-loading.md`
**Also fixes:** BUG-009 (plan generation <15s visible)
**Independent**

- [ ] [API/DB] Update `app/api/plans/generate-v2/route.ts` to stream: emit one JSON chunk per slot as each meal resolves, in planning-day order (Mon Breakfast → Mon Lunch → … → Sun Dinner); first meal must be visible to client in <5s; shopping list generation remains async and non-blocking.
- [ ] [UI] Generation loading screen: full viewport (replaces planning form on submit, not a modal); p1-cream background; header "Rasa is sorting your week..." (Playfair Display, text-xl, font-bold, p1-dark, left, pt-8, px-4); sub-label below header (p1-brown, text-sm) rotates through 3 messages every 4s with 200ms cross-fade: "Picking recipes you'll actually want to make." / "Making sure nothing repeats too soon." / "Thinking about what goes well together."
- [ ] [UI] Streaming slot rows: one row per slot in planning-day order; State 1 pending (day label + meal chip + animate-pulse skeleton bar ~140px); State 2 generating (day label + meal chip + "Finding..." italic, opacity animate-pulse); State 3 done (day label + meal chip + recipe name + ✓ p1-forest, 100ms fade-in); rows stay in planning-day order regardless of stream arrival order.
- [ ] [UI] Success state: all rows reach State 3 → list fades out (200ms) → centered "Your week is sorted. ✓" (Playfair Display, text-2xl, font-bold, p1-dark) + "[N] meals, zero decisions left." (text-sm, p1-brown) fades in → auto-nav to Planner tab at 1.5s. Fallback: show "Go to Planner" button after 3s if nav fails.
- [ ] [UI] Error states: full failure → replace header with "Hmm, something didn't work." + "Give it one more try?" + full-width "Try again" button (p1-terra); partial failure → failed rows show inline "Couldn't find a [Meal type] for [Day] — try again?" + "Continue with what we have" ghost link at bottom.

---

## FEAT-S02-004 — Home Today Card

**Spec:** `docs/ux/home-today-card.md`
**Depends on:** FEAT-S02-000

- [ ] [UI] Remove "This Week" day-strip widget from home tab.
- [ ] [UI] `TodayCard` component: always p1-terra (#C4522A) background, white text, rounded-xl, px-4 py-4, full-width; 8 scenarios driven by plan state + time-of-day (before 12pm = morning, 12–4:59pm = afternoon, 5pm+ = evening) + cooked flags. See spec for full scenario map. Hero meal shown at full size; secondary meals in "Also today:" compact section (tappable to swap hero). CTA layout: Row 1 full-width "Start Cooking" (white bg, p1-terra text); Row 2 two-col "See Recipe" | "Swap" (outlined white); Row 3 ghost text links "Skip" · "Mark as Cooked" (white, text-sm, opacity 70%, underlined).
- [ ] [UI] Afternoon nudge (Scenario 4+5): if morning meal was planned but not cooked and current time ≥ 12pm, show "Did you make breakfast? / [Recipe name] / Yes, mark it cooked" below CTAs. Tapping marks that meal cooked without navigating away.
- [ ] [UI] `TomorrowPreview` component below TodayCard: hidden if 0 meals tomorrow (except Sunday evening nudge); compact list for 1–3 meals with no Breakfast; elevated "Prep tonight for tomorrow" card (p1-card bg, p1-dark text) if tomorrow has Breakfast and it is evening; passive list if morning/afternoon; Sunday evening planning nudge ("Sunday already. Plan next week while it's fresh? [Let's plan →]") if no next-week plan.

---

## FEAT-S02-005 — Onboarding Q1 dietary refresh

**Spec:** `docs/ux/onboarding-q1-dietary.md`
**PM review override:** 8 chips not 6 — add "No red meat" (`no_red_meat`) and "No raw fish" (`no_raw_fish`) after Nut-free.
**Independent**

- [ ] [UI] Update Q1 onboarding component: heading "Any dietary rules we should always follow?" (Playfair Display, text-2xl, font-bold, p1-dark, left); sub-label "Pick everything that applies — or skip if you eat everything." (Nunito, text-sm, p1-brown); 8 chips in wrapping row — unselected: p1-surface bg + p1-dark text; selected: p1-terra bg + white text; nothing pre-selected by default; returning user: pre-fill from saved `dietary_flags`. Chips: Vegetarian, Vegan, Halal, Gluten-free, Dairy-free, Nut-free, No red meat, No raw fish.
- [ ] [UI] "Anything else?" label + textarea (always visible; placeholder "e.g. no shellfish, low-sodium for a family member"; 64px min height; p1-border border; p1-terra focus ring 3px 50% opacity); "Next →" button (p1-terra, full-width, h-10, always enabled); "Skip for now" ghost link (p1-brown, text-sm, underlined) — same effect as Next with 0 selections.
- [ ] [API/DB] Accept `no_red_meat` and `no_raw_fish` as valid `dietary_flags` values in the preferences save route. Update plan engine prompt to treat these as exclusion constraints (no red meat = no beef, lamb, pork, venison; no raw fish = no sushi, sashimi, ceviche, tartare).

---

## FEAT-S02-006 — Recipe detail prep section

**Spec:** `docs/ux/recipe-prep-section.md`
**Independent**
**Token note:** spec references `p1-border-lt` — use `p1-surface` (#EDE0D0) instead (same hex, correct token name).

- [ ] [UI] `PrepAheadSection` component: show only if `recipe.prep_ahead` is populated (non-null, non-empty); hide entirely otherwise — no empty state. Placement: below ingredients list, above method/cook steps. Full-width divider (1px, p1-surface) above heading. Heading "Prep ahead" (Nunito, text-base, font-semibold, p1-dark). Intro line "Do this tonight and tomorrow morning takes [X] minutes." (p1-brown, text-sm); if `assembly_time_mins` is null use "Do this tonight and tomorrow morning is easy."; if `assembly_time_mins` is 0, omit intro line. Tonight's prep: numbered list if multi-step, paragraph if single sentence (Nunito, text-sm, p1-dark; list numbers p1-terra, font-semibold). Assembly time line "Assembly time tomorrow: [X] min" (Nunito, text-xs, p1-brown) — show only if `assembly_time_mins` populated and > 0. No card surface — inline section, same px-4 as rest of recipe detail.

---

## FEAT-S02-007 — Recipe bank import + browsing

**Spec:** `docs/ux/import-modal.md` (existing spec, unchanged)
**Independent**

- [ ] [UI] Import modal: recipe name text field; recipe_type chip group (Main / Side / Salad / Complete meal, single-select); meal_type chip group (Breakfast / Lunch / Dinner, multi-select); optional source_url text field; large paste textarea for recipe text; Save button; no loading state — save is fast (no AI).
- [ ] [API/DB] `POST /api/recipes/import`: save to `recipes` table with `source='user_imported'`, `recipe_type`, `meal_type[]` tags, `source_url`; scoped to `anon_id`; no AI call at import time; return saved recipe row. Only `Main` and `Complete meal` recipe_types are eligible for plan generation (`excluded_from_plans` logic — Sides and Salads default to `excluded_from_plans=true`).
- [ ] [UI] Recipe bank page: filter chips by recipe_type and meal_type; "Yours" badge on `source='user_imported'` cards; source_url link icon on card opens URL in new tab if present.

---

## FEAT-S02-008 — Bank-first plan generation + protein-first prompt

**Spec:** `docs/prd/feat-003-bank-first-generation.md`
**Depends on:** FEAT-S02-000 (meal types), FEAT-S02-007 (bank exists)

- [ ] [API/DB] Update `generate-v2` prompt: fetch user's recipe bank filtered by meal_type and `excluded_from_plans=false`; inject bank recipes as a candidate list in the AI prompt; instruct AI to prefer bank recipes, generate new ones only when bank has <2 candidates for a given slot. Pass `dietary_flags` (including `no_red_meat`, `no_raw_fish`) and `health_goals` as hard constraints.
- [ ] [API/DB] Protein-first heuristic in prompt: "For each meal slot, pick a protein source first, then choose complementary carbs and vegetables."
- [ ] [API/DB] "Won't make again" exclusion: filter `verdict='wont_make_again'` OR `excluded_from_plans=true` recipes from bank candidate list before injecting into prompt.

---

## BUG-013 — Cook mode routing (CRITICAL)

Fix alongside feature work — does not depend on schema migration.

- [ ] [UI] "Let's cook" / "Start Cooking" on home card → navigates to step-by-step cook mode (`/cook/[meal_id]`). Currently routes to wrong page or breaks.
- [ ] [UI] "Start Cooking" on recipe detail → enters cook mode for that recipe.
- [ ] [UI] Do not add new cook mode features — fix routing only.
- Files to check: `app/kitchen/page.tsx`, `app/recipes/[id]/page.tsx`, cook mode component, home card CTAs.

---

## Done this sprint

- [x] BUG-014: Recipe bank cross-user data leak — done 2026-05-11 | backend-engineer
- [x] BUG-002, 003, 004, 005, 006, 007: Backend data integrity bugs — done 2026-05-07 | backend-engineer
- [x] BUG-001: Direct Supabase calls in home + recipe pages — done 2026-05-07 | frontend-engineer
- [x] BUG-008: Q1 onboarding validation — done 2026-05-10 | frontend-engineer (UX-026 replaces the visual design but validation fix stays)
- [x] Test framework: Vitest + RTL + Playwright (54 E2E tests) — done 2026-05-08 | test-engineer
